# dotnet-rag-api
###.NET RAG API

Work in progress. I'm building a RAG API in ASP.NET Core with EF Core, PostgreSQL and pgvector, writing the code myself rather than generating it, to close the gap between understanding backend concepts and being fluent in them.
